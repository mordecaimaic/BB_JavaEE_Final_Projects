<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">

    <insert id="insertUser"
            parameterType="com.example.model.User"
            useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users
            (username, password, phone, email, student_id, department, role)
        VALUES
            (#{username}, #{password}, #{phone}, #{email}, #{studentId}, #{department}, #{role})
    </insert>

    <select id="selectByUsername"
            parameterType="String"
            resultType="com.example.model.User">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <!-- 根据用户名取出密码哈希 -->
    <select id="getPasswordHash"
            parameterType="String"
            resultType="String">
        SELECT password
        FROM   users
        WHERE  username = #{username}
    </select>
</mapper>