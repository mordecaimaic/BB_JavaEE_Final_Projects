<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.example.model.User">
        <id property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="studentId" column="student_id"/>
        <result property="department" column="department"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insertUser" parameterType="com.example.model.User">
        INSERT INTO users (username, password, phone, email, student_id, department, role, created_at)
        VALUES (#{username}, #{password}, #{phone}, #{email}, #{studentId}, #{department}, #{role}, NOW())
    </insert>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT user_id, username, password, phone, email, student_id, department, role, created_at
        FROM users
        WHERE username = #{username}
    </select>

    <select id="selectByUsernameAndRole" resultMap="UserResultMap">
        SELECT user_id, username, password, phone, email, student_id, department, role, created_at
        FROM users
        WHERE username = #{username} AND role = #{role}
    </select>

    <select id="getPasswordHash" resultType="java.lang.String">
        SELECT password
        FROM users
        WHERE username = #{username}
    </select>

    <select id="selectUserById" resultMap="UserResultMap">
        SELECT user_id, username, password, phone, email, student_id, department, role, created_at
        FROM users
        WHERE user_id = #{userId}
    </select>

    <update id="updateUserProfileDetails" parameterType="com.example.model.User">
        UPDATE users
        SET phone = #{phone},
            email = #{email}
        WHERE user_id = #{userId}
    </update>

</mapper>